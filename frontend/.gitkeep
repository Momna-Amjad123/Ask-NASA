# Frontend directory placeholder.
#
# The Next.js + React + TypeScript application shell is created in Phase 1
# (see docs/ROADMAP.md, "Phase 1 — Frontend + Backend Skeleton").
# Phase 0 only establishes this directory.
